services:
    # web-api:
      #  image: template-backend:latest
       # build:
        #    context: ./Backend
         #   dockerfile: Dockerfile
        #container_name: template-backend
        #ports:
        #    - "5000:8080"
        #environment:
        #    - ASPNETCORE_ENVIRONMENT=Development
        #    - ConnectionStrings__Template=Host=postgres-db;Database=${DB_NAME};Username=${DB_USER};Password=${DB_PASSWORD}
        #depends_on:
        #    - postgres-db

    postgres-db:
        image: postgres:18-alpine
        container_name: sipo-if-db
        ports:
            - "6666:5432"
        environment:
            - POSTGRES_DB=${DB_NAME}
            - POSTGRES_USER=${DB_USER}
            - POSTGRES_PASSWORD=${DB_PASSWORD}
        volumes:
            - postgres_data:/var/lib/postgresql

volumes:
    postgres_data:
